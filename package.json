{
  "name": "telegram-multi-uploader",
  "version": "1.0.0",
  "description": "Telegram bot for uploading up to 2GB files to Pixeldrain, GoFile, and storage.to using a local Bot API server",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "grammy": "^1.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
